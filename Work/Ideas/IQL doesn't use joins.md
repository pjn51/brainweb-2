# IQL doesn't use joins. 
Unlike [[SQL]], [[IQL]] doesn't use `JOIN` to get data from multiple locations. Instead, we use subqueries, making them quite important in this query language.

---
#idea/compsci/sql 
